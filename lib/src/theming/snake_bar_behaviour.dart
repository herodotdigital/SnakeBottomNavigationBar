enum SnakeBarBehaviour {
  /// use [SnakeBarBehaviour.floating] style if you want to
  /// separate [SnakeNavigationBar] from bottom side
  floating,

  /// [SnakeBarBehaviour.pinned] is default [SnakeNavigationBar] style
  /// which is pinned to bottom side
  pinned
}
